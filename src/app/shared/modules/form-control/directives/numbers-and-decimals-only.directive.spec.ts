import { NumbersAndDecimalsOnlyDirective } from './numbers-and-decimals-only.directive';

describe('NumbersAndDecimalsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbersAndDecimalsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
